<!--
OWNER: Knowledge Base Writer (only). Content from UI/UX.
This file is a SINGLETON — this topic lives here and nowhere else. Feature
docs must link to specific sections here, never restate this content.
Visual tokens and component styling live in design-system.md; this file
covers how the interface behaves.
-->

# Pattern: UI Patterns

## When to use
Any user-facing feature — screens, forms, messages, navigation.

## Convention
{the interaction conventions — e.g. navigation, forms and validation, feedback
and notifications, empty/loading/error states, destructive actions, tables and
lists, accessibility baseline, responsive behavior}

## Minimal example
```
{short example}
```
